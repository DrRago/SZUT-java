package de.szut.dqi14.gahr.E2.WinterHell;

class LittleSnowflake extends Snowflake {
	
	public LittleSnowflake(float x, float y, float speed) {
		mX = x;
		mY = y;
		mSpeed = speed;
		design = "*";
	}
}