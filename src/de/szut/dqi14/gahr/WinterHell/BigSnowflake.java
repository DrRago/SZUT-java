package de.szut.dqi14.gahr.WinterHell;

class BigSnowflake extends Snowflake {
	
	public BigSnowflake(float x, float y, float speed) {
        mX = x;
        mY = y;
        mSpeed = speed;
        design = "X";
    }
}
